# misc-api URL Rewrite (Optional)
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # 禁止直接访问 config 目录
    RewriteRule ^config/ - [F,L]
</IfModule>

# 404 错误页
ErrorDocument 404 /404.php

# 禁止目录浏览
Options -Indexes
